# v1.20 · 别让一次改动悄悄推翻上一次的确认 · 账户组

> 状态:**待评审**(PRD + preview 阶段 · 尚未写代码)
> 目标版本:app.version = 1.20.0(已 bump)
> 来源:[issue #17](https://github.com/LuoDi-Nate/financial-management/issues/17) —— 一位连续用满两个月的自建用户提的四条
> 骨架:照 [`prd/_TEMPLATE.md`](_TEMPLATE.md) 走(大版本 · 全套 12 节)
>
> **已剥离**:第 3 条(自定义支出分类)→ [#18](https://github.com/LuoDi-Nate/financial-management/issues/18) 独立立项;
> 第 1 条里的 AI 复盘缓存 bug → 已作为补丁在 **v1.19.16** 发布
>
> **评审中的一次方向修正(2026-09-07)**:提交者的第 2 条,我原本写成「活期账户合并统计 + 一个开关」。
> 维护者评审时把它**抽象成了「账户组」** —— 「活期」只是一个推荐分组,不是一个专门功能。
> 这个抽象更好,而且好在一处硬的地方(见 §0.2)。原方案的开关设计也被否掉了:**不需要开关**。

---

## 0 · 这一版在解决什么

### 0.1 背景与现状

三条诉求看起来不相干,底下是同一件事:**系统自己知道、但没告诉用户的东西**。

| 系统知道 | 用户看到 |
|---|---|
| 你上个月确认过的余额,刚被这个月的一次收入改动改掉了 | 什么都没有 |
| 你有十几个活期账户,它们之间的划转对「家里有多少钱」毫无意义 | 归因里十几条互相抵消的噪声 |
| 归因在回答「这个月钱变了多少、是谁造成的」 | 一句「没有录入」 |

**这不是「体验不够好」,是三处明确的信息缺口** —— 每一处底下都有具体的、可复现的现象。

### 0.2 第二条为什么从「活期合并」变成「账户组」

`transfer` 表有 `from_account_id` 与 `to_account_id` 两端。而今天划转的抵消**只有两档**:

| 层级 | 划转怎么算 | 后果 |
|---|---|---|
| 单账户 | 全部算外部现金流 | 归因里满屏互相抵消的噪声 —— 正是提交者抱怨的 |
| 全家 | 全部抵消 | 看不到任何结构 |

**中间没有档位。** 账户组正好补上这一档:

> **两端都在组内的划转 → 内部流转,不计入该组的现金流;一端在组外 → 照算。**

于是「**这个组的收益率**」有了**严格定义**,而不是把几行加起来。
原方案「合并统计 + 开关」给不出这个定义 —— 这才是它被否掉的真正原因,不是「不够通用」。

顺带:归因现有 6 个维度(`账户 / 资产类型 / 成员 / 平台 / 币种 / 账户类型`)**全是系统推导的**。
账户组会是**第一个用户自定义的维度**。

### 0.3 服务谁

**主要受益者:自建用户与维护者本人**(会主动改数据、会看归因的那类)。
非技术家人间接受益 —— 分组 A 的提示让她们不会在不知情的情况下推翻已确认的数。

### 0.4 主推动力

**分组 A(别让一次改动悄悄推翻上一次的确认)是主推动力。**

它是唯一一条「用户已经因此付出了真实代价」的:提交者为此**把上个月账期重新打开重填了一遍**。
账户组(B)与归因空态(C)是同一次反馈里顺带解决的,不是这一版存在的理由 ——
如果时间只够做一件事,做 A。

### 0.5 本版新引入的名词

| 名词 | 含义 |
|---|---|
| **余额校准状态** | 某账户在某期「本月余额有没有被人确认过、什么时候确认的」。不是新数据,是把已有 `period_snapshot` 的提交时间**显式说出来** |
| **账户组** | 一个**可选的叠加聚合层**,含 1..N 个账户。**它不是账户的属性** —— 账户默认不属于任何组;一个账户**最多属于一个组** |
| **组内流转** | 两端账户都在同一个组内的划转。对该组是内部搬钱,**不计入组的现金流** —— 组级收益率的定义基础 |

其余名词(镜头 / 开账基线 / 人赚钱赚 / 定格 / 随时可取)见 `AGENTS.md` 领域概念节,不重复。

---

## 1 · 调研

**不适用 · 本版无外部平台或协议依赖。**

三条诉求全部落在既有数据与既有页面上,不引入任何第三方能力。
账户组会复用现有的 `AccountLiquidity`(推荐分组用)与 `transfer` 两端信息,都是自家代码。

---

## 2 · 用户故事

**全部引用提交者原话。**

### 2.1 第 1 条 · 收入与余额校准的先后关系

> 「一旦后期二次修改收入,错过或者影响了这个先后关系,或者没有重新去校验余额,就会导致**已经校准的余额失效**!
> ……我觉得至少有个强烈的提醒……因为校验后的余额一般来说,实际上一旦录入一次就不会再变更了,
> 或者说是它的**稳定性比收入的录入要高**。」

> 「而且对于这种修改之前已关闭月份的场景,好像计算有问题……
> 因为我把之前的数据修改后,在仪表盘上查看,**始终好像还是之前的数据**,并没有更新。」

**照他的路径在 beta 上用真实浏览器复现过**(重开上月 → 删一笔收入 → 重录一笔金额不同的 → 重新关账):

| 他以为 | 实际 |
|---|---|
| 已校准的余额「失效」 | **被直接改掉了** —— 期末余额跟着那笔收入同步变化,页面零提示 |
| 仪表盘数字没更新 | **更新了** —— 没更新的是 AI 复盘那段解读(已在 v1.19.16 修掉) |

复现还坐实了一个**他没提、但一定会让人困惑**的条件:

> **当月还没填余额的账户,它上面的变动不会进「本期净资产变化」。**
> 复现时删掉的那笔落在一个当月未填余额的账户上(19 个账户填了 18 个),
> 于是它对仪表盘毫无影响。**「改了数据但数字不动」确实会发生**,而页面没有解释这个条件。

**他那句「校验后的余额稳定性比收入录入高」是这一版的设计原则** ——
提示该保护的是**后确认的那个**,不是先录的那个。

### 2.2 第 2 条 · 活期账户按总计统计 → 账户组

> 「……所以我建议,是否可以将所有活期账户在**录入时仍然按账户录入,
> 但在最终统计收益、计算等场景下汇总展示**。
> 这实际上跟把当前已有的设计——所有支出都仅记录到一个总额在一定程度上是一个道理。」

他要的**不是少几个账户,是少看几行噪声** —— 注意他自己明确写了「录入时仍然按账户录入」。

维护者评审时把它抽象成账户组,并补了一条他的真实场景:

> 「比如我有 10 个活期账户,我只想把其中 9 个合并到我们说的『活期』里面,剩余 1 个可以自由管理。」

所以**不能是一个开关** —— 必须能选哪些进组。

### 2.3 第 4 条 · 归因看不懂

> 「最后就是那个什么归因……这个归因确实没搞明白怎么修改或者怎么玩。
> 反正看着**显示没有录入**,也不知道怎么改,能否简化/新增入口或加点什么指导之类的?」

一个需要看文档才会用的功能,等于没做。

---

## 3 · FR

**preview**:[`preview/v1.20/index.html`](../preview/v1.20/index.html)(双端真浏览器渲染核对)

### 3.1 分组 A · 别让一次改动悄悄推翻上一次的确认

| # | 要求 |
|---|---|
| FR-450 | 在**当月已校准过余额**的账户上新增 / 删除 / 修改收支时,**当场**告诉用户:这个账户的余额是什么时候校准的、这笔改动会把它从多少改成多少 |
| FR-451 | 提示里要给**下一步**,不是只给警告 —— 至少「知道了,余额按新值走」和「去重新校准这个账户」两条路 |
| FR-452 | 账户在填报页上要能**一眼看出**「本月余额校准过没有 / 什么时候校准的」,不用点进去 |
| FR-453 | **当月未填余额的账户,其变动不进「本期净资产变化」** —— 这个条件必须写在页面上。当用户改的恰好是这类账户时,要主动说明 |
| FR-454 | 重新打开已关账月份时,提示要**列出会被重算的东西**,而不是只说「会重算之后所有指标」 |

**FR-450 的边界**:提示是**信息**不是**拦截**。按下去就该过,不设二次确认弹窗 ——
这个 app 的用户是家里人,不是审批流。

### 3.2 分组 B · 账户组

#### 概念与约束

| # | 要求 |
|---|---|
| FR-460 | 可以创建**账户组**,一个组包含 1..N 个账户。**组是可选的叠加层,不是账户的属性** —— 账户默认不属于任何组 |
| FR-461 | **一个账户最多属于一个组。** 这是硬约束不是偏好:归因有恒等式「基准 + 人赚 + 钱赚 + 开账基线 = 本期净变化」,一个账户进两个组就会双计,恒等式当场崩 |
| FR-462 | **记账完全不变** —— 仍然按账户填余额、记收支、导截图。**组不是记账实体**,不能对着组录任何东西 |
| FR-463 | 组可增删改;**删组之后成员账户回到「未分组」**,数据一行不动 |

#### 展示与下钻

| # | 要求 |
|---|---|
| FR-464 | 在归因 / 收益 / 透视 / 分布里,**账户维度按组折叠渲染**:一个组 = 一个维值;**未分组账户各自一个维值**,与组并列 |
| FR-465 | 折叠是**默认状态**,并且**支持下钻**展开到成员账户。默认必须折叠 —— 不然就违背了 #17 的原始诉求 |
| FR-466 | **没建任何组时,所有页面与 v1.19.16 逐字一致。** 零影响是结构上必然的,**不需要开关** |

> **为什么不是「新增一个 group 维度、account 维度不变」**:那样用户在账户维度下**仍然会看到 9 行噪声**,
> 还得记住「要换个维度看」。折叠必须发生在账户维度自己身上。

#### 口径

| # | 要求 |
|---|---|
| FR-467 | **组内流转不计入该组的现金流**(两端都在组内);**一端在组外的照算**。这是组级收益率的定义 |
| FR-468 | 组成员关系**跟账期走**:已关账月份的历史数字,**不会因为今天改了组而变**;只有重开该月再关账才按新分组重算(与 v1.12「重开后再关账 = 重新定格」同一条规则) |

#### 建组体验

| # | 要求 |
|---|---|
| FR-469 | 建组时**系统只推荐、不自动创建**:按 `AccountLiquidity.LIQUID` 推荐「随时可取」,按 `isLiability()` 推荐「负债」。**默认全选推荐成员,用户可取消勾选**(他的原话:10 个里只想合并 9 个) |
| FR-470 | **允许跨类型建组**(把股票和现金放一组),但建组时**如实提示代价**:这个组的收益率会混口径。不拦 —— 限制用户的东西我们经常猜错;但不告知代价是另一回事 |

### 3.3 分组 C · 归因要能自己看懂

| # | 要求 |
|---|---|
| FR-480 | 归因区**空态**要说清三件事:它在回答什么问题、为什么现在是空的、下一步做什么 |
| FR-481 | 用一句话解释归因在回答的问题,放在功能旁边,不放文档里 |
| FR-482 | 入口要在用户会经过的路径上,而不是要找 |

### 3.4 关键文案(文案即功能)

| 场景 | 文案要点 | 为什么这句话本身就是功能 |
|---|---|---|
| FR-450 提示 | 「这个账户的余额在 **MM-DD 校准过**,这笔改动会把它变成 **X**」 | 少了「校准过」三个字,用户不知道自己在推翻什么 |
| FR-453 提示 | 「这笔改动**不会**反映到本期净资产变化 —— 该账户本月还没填余额」 | 这一句就是提交者困惑的解药 |
| FR-467 说明 | 「组内互相转账**不算**这个组赚了或亏了」 | 不说清,用户会以为我们把钱算丢了 |
| FR-470 提示 | 「这个组里既有股票又有现金,**它的收益率会混口径**」 | 不说清就是把取舍藏起来 |
| FR-480 空态 | 「这块在回答:这个月家里的钱变了多少,分别是哪些账户、哪类资产造成的」 | 这一句就是 FR-481 本身 |

### 3.5 流程图

**不画。** preview HTML 已覆盖全部交互。
唯一的状态机(账期开/关/重开)是既有行为;FR-468 复用它的定格规则,不新增状态转移。

---

## 4 · 不做什么

### 非目标

- **不做自定义支出分类 / 支出报表** —— 已拆到 [#18](https://github.com/LuoDi-Nate/financial-management/issues/18)
- **组不是记账实体** —— 不能对着组填余额、记收支、导截图。想那么做的是另一种设计(见下)
- **不做多套分组方案** —— 一个账户属多个组、或多套并行的分组镜头,留到有真实需求再说
- **不加二次确认弹窗** —— 分组 A 是提示,不是审批
- **不动账期状态机** —— FR-454 只改文案,FR-468 复用既有定格规则

### 评审中被否掉的方案(记下来,免得再提)

**「真合并成一个可记账的账户」** —— 9 个账户变 1 个「活期总账」,每月只填一个总数。
它能真正减少录入次数,但被否,因为:

1. **会打断 v1.4 截图导入** —— 截图来自单个 APP(支付宝 / 微信 / 某银行),天然按账户;
   `HoldingImportService.startOrResume(accountId)` 一次导入绑一个账户。合并后它无处可落,
   补救方案都等于把合并又拆开
2. **不可逆** —— 9 个账户的历史快照要么两套并存、要么合并掉
3. **提交者要的不是它** —— 他原话是「录入时仍然按账户录入」
4. 它省的是「敲 8 个数」,而截图导入本来就在解决「敲」

**「填一个总数,按上期比例分摊到各账户」** —— 直接否。**那是编数据**,与本项目的诚实原则冲突。

### 撞上「刻意不做」清单的

**没有。** 账户组贴着「逐笔流水账」的边但方向相反:提交者要的是**少录**(别逼他录划转),
不是多录。与清单一致,不是例外。

---

## 5 · 10 分钟约束核对

| FR | 对月度录入负担的影响 | 判定 |
|---|---|---|
| FR-450 / 451 | **+0**。只在用户**已经在改数据**时多显示一段文字;不改就永远看不到 | 通过 |
| FR-452 | **+0**。填报页多一个状态标签,纯阅读 | 通过 |
| FR-453 | **+0**。纯提示。附带的「顺手把本月余额填了」是**入口**不是**要求** | 通过 |
| FR-454 | **+0**。重开账期是低频管理动作,只改文案 | 通过 |
| FR-460 ~ 470 | **+0**(录入不变)。建组是**一次性**动作,之后每月零成本 | 通过 |
| FR-480 ~ 482 | **+0**。纯阅读 | 通过 |

**结论:没有任何一条拉高月度录入负担。**

**诚实说一句**:账户组也**不降低**录入次数 —— 他还是要填 10 个余额。
它降低的是**看的噪声**,不是**敲的次数**。真想降录入次数只有被否掉的那条路,代价见 §4。

---

## 6 · 存量影响

| 维度 | 这一版的影响 |
|---|---|
| **schema** | 分组 A / C **零 schema 改动**。分组 B 需要新表:账户组 + 成员关系 + **成员关系的账期定格**(FR-468)。**只加表,不改既有表** |
| **口径** | 见下 |
| **UI** | **没建任何组的用户,统计/归因/透视一个像素都不变**(FR-466)。分组 A / C 新增提示与空态文案 —— **只增不改**,不动任何既有数字与布局 |

### 口径变更声明

**分组 A / C:不涉及口径变更。**

**分组 B:涉及,但只在用户建了组之后才生效。**

- **改了什么**:① 账户维度的**呈现**从「一账户一行」变成「一组一行 + 未分组账户各一行」;
  ② **组内流转不计入该组现金流**(FR-467)—— 这是真正的口径变化,它改变了组级收益率的分母
- **旧数据会不会跟着变**:**不会** —— FR-468 规定组成员关系跟账期定格,已关账月份的历史数字不动。
  只有重开该月再关账才按新分组重算。这与 v1.12「重开后再关账 = 重新定格」是同一条规则
- **零差异基线**:**没建任何组时,所有页面必须与已发布 tag `v1.19.16` 逐字一致**。
  基线取 tag 而不是当前工作区,否则分不清代码差异与数据漂移
- **调用方清单**:归因 / 收益 / 透视 / 分布四条路径靠人列**必漏** → 落 grep 护栏,
  扫「所有按账户聚合的入口是否都过了同一个折叠层」

### 6.1 · 逐组件盘查:两个核心页面(2026-09-08 · 维护者要求 · 已复核修订)

> 维护者原话:「我们做的『账户分组』,在 dashboard 和报告页面,**每一个组件**是否应该有影响?
> 你**逐个列出**两个核心页面的所有组件,一个个组件进行分析。」
>
> 复核要求:「你这个输出结果的结论是怎么得到的?说明下原因,重新 review 下你的结论。」

#### 6.1.0 · 方法:清单怎么来的、判据怎么定的

**第一步 · 组件清单从哪来。** 不凭印象列。第一版取自两页的**长文目录**(`tocItems`),
理由是那是页面结构的显式声明。**复核时发现这个来源不够** —— 目录只收「值得跳转的锚点」,
于是漏掉了三类东西:

| 漏的类型 | 实例 | 为什么会漏 |
|---|---|---|
| 没有独立 `id` 的块 | dashboard「资产配置」饼图(藏在 `#dash-trend` 里) | 它和净值曲线共用一个 section |
| 不是 section 的交互件 | dashboard **账户多选筛选器** | 它是个 `<details>` 表单,不需要锚点 |
| 一个 section 内的多个块 | `#sec-risk` 有 2 张图;`#sec-structure` 有 4 块 | 目录一个条目 = 页面一个 section ≠ 一个组件 |

**修正后的清单来源 = 三个来源取并集**:① `tocItems` ② 各 fragment 里的 `<section id>`
③ **所有 `eyebrow` / `<h3>` 标题块**(这一条才捞出了「第三表」「流动性分层」「按成员」这些)。

**第二步 · 判据。** 第一版用的是「模板里有没有 `accountName`」—— **这个判据是错的**。
因为**同样是列账户名,用途完全不同**:「最大变动账户」是给你**看**的,
「哪些账户没填余额」是让你去**填**的。前者该跟分组走,后者跟了就废了。

换成**「用户看完这个组件,下一步要干什么」**才对:

| 类 | 它在回答什么 | 跟不跟分组 | 为什么 |
|---|---|---|---|
| **看** | 「我的钱怎么分布 / 赚在哪 / 亏在哪」 | **跟** | 这正是 issue #17 的诉求:活期账户之间的来回划转是噪声 |
| **做** | 「我接下来该去点哪、填哪、从哪转到哪」 | **不跟** | **组不是一个能记账的东西**(§0.5)。给一个点不进去、转不了账的组名 = 把功能变没 |
| **险** | 「我的钱是不是压在一个篮子里」 | **不跟** | 分组是**观察偏好**,不改变真实世界的对手方 |

**第三步 · 逐条找数据源证据,不靠模板推断。** 复核时对每个判「不动」的组件都回到了代码:

| 组件 | 证据 | 结论 |
|---|---|---|
| 资产配置饼图 | `AllocationSlice(String accountType, …)` | 按**账户类型**聚合,不是账户 |
| 负债结构 / 风险等级 | `debtValues` 是 `TrendPoint` 时序;`riskDistribution` 来自 `familyDiagnose` | 时序 / 按风险等级 |
| 财富水位 · 储蓄能力 | 模板里 `accountName|accountId` 出现 **0 次** | 纯家庭级 |
| 按成员分布 | `computeMemberAllocation` 按 `primary_owner_member_id` 聚合,**并且**要用 `accountId` 反查 `Account` | 按 owner;**且必须传原始行** —— 组行 `accountId=null` 会让组里的钱从饼图里整片消失 |
| 资产洞察 | `topAccLabel = p.accountName()` 喂给 `ConcentrationCalculator.line(…, thresholdPct)` | 是**集中度风险判定**,不是分布展示 → 「险」 |

#### 6.1.1 · 「险」这一类是最重要的发现,而且差点整个漏掉

集中度 HHI = Σ(份额²)。5 个各占 4% 的活期账户 → HHI ≈ 0.008;
合成一个占 20% 的组 → 0.04,**翻了 5 倍**。
指标会告诉用户「你的集中度风险暴增了」,而他**什么都没做,只是换了个看法**。

**存款保险 50 万上限是按「银行」算的,不按用户的分组习惯算。**
5 张不同银行的卡合成一组,不会让它们变成一家银行 —— 所以这类指标必须钉在真账户上。

#### 6.1.2 · Dashboard · 12 个组件

| # | 组件 | 聚合维度 / 数据源 | 类 | 结论 |
|---|---|---|---|---|
| 1 | 筛选器 · 区间 / 币种 / as-of | 查询参数 | — | **不动** |
| 2 | **筛选器 · 账户多选** | `allAccounts` 勾选 | 交互 | ⚠️ **要改**(FR-486)· **复核时才发现漏了** |
| 3 | 概览 KPI 带 `#dashboard-region` | 家庭级合计 | — | **不动**。合计与怎么分组无关 |
| 4 | 资产洞察 `#dash-insight` | 含「最大单账户集中度」 | **险** | **不动** |
| 5 | 人赚 vs 钱赚 `#dash-cashflow` | 家庭级 | — | **不动** |
| 6 | 归因复盘 `#dash-attribution` | **按账户** | 看 | ✅ 已改(FR-464/465/476) |
| 7 | 收支趋势 `#dash-cashflow-trend` | 家庭级时序 | — | **不动** |
| 8 | 净资产趋势 `#dash-trend` | 家庭级时序 | — | **不动** |
| 9 | **资产配置饼图**(`#dash-trend` 内) | `AllocationSlice.accountType` | — | **不动** · **复核时才补进清单** |
| 10 | 按成员分布 `#dash-member` | 按 owner | — | **不动**,且**必须继续吃原始行**(见上表) |
| 11 | **按账户分布 `#dash-dist`** | **按账户** | 看 | ⚠️ **改**(FR-482) |
| 12 | **账户列表 `#dash-list`** | **按账户** | 看 + 做 | ⚠️ **改**(FR-483) |
| 13 | 资产透视 `#lens-section` | 按维度 | 看 | ✅ 已改(加 `group` 维度) |

#### 6.1.3 · Reports · 21 个块

| # | 组件 | 聚合维度 / 数据源 | 类 | 结论 |
|---|---|---|---|---|
| 1 | 封板 · 月度大账 / 瀑布 | 家庭级 | — | **不动** |
| 2 | 封板 · **完整性检查** | 列「哪些账户没填余额」 | **做** | **不动**。待办清单,合并了不知道去填谁 |
| 3 | 封板 · **最大变动账户** | **按账户** | 看 | ⚠️ **改**(FR-484)· 且必须读**该期定格** |
| 4 | 结构 · **集中度 HHI / Top1** | 按账户份额 | **险** | **不动** |
| 5 | 结构 · **流动性分层** | 按流动性等级 | — | **不动** · 复核时才补进清单 |
| 6 | 结构 · **按成员** | 按 owner | — | **不动** · 复核时才补进清单 |
| 7 | 结构 · **按资产大类** | 按 `AccountClass` | — | **不动** · 复核时才补进清单 |
| 8 | 趋势 `#sec-trend` | 家庭级时序 | — | **不动** |
| 9 | 收益概览 `#reports-region` | 家庭级 | — | **不动** |
| 10 | 第一表 · 本金 vs 投资损益 | 家庭级 | — | **不动** |
| 11 | 第二表 · 负债结构(`#sec-risk`) | 时序 | — | **不动** |
| 12 | 第三表 · 风险等级分布(`#sec-risk`) | 按风险等级 | — | **不动** · 复核时才拆出来 |
| 13 | 第五表 · 资产配置对照 `#allocation-diff` | 按资产类别 | — | **不动** |
| 14 | 调仓建议 `#ai-rebalance` | 「从 X 调出 → 到 Y」 | **做** | **不动**。**组不能转账** |
| 15 | 再平衡计划下拉 `_rebalance-plan` | `planAccounts` | **做** | **不动**。划转要落到能记账的地方 |
| 16 | **第四表 · 账户级收益 `#sec-accounts`** | **按账户** | 看 + 做 | ⚠️ **改**(与 dashboard #12 同源) |
| 17 | 财富水位 `#sec-wealth` | 家庭级 | — | **不动** |
| 18 | 财富水位 · 基准推导透明 | 家庭级 | — | **不动** · 复核时才补进清单 |
| 19 | 储蓄能力 `#sec-savings` | 家庭级 | — | **不动** |
| 20 | 支出构成 `#sec-expense-mix` · 聚合 | 按支出类目 | — | **不动** |
| 21 | 支出构成 · **逐笔明细行** | `mixDetailRows` 带 `accountName` | **做** | **不动** · **复核时才发现有这一半** |
| 22 | 期间下钻弹窗 `_drilldown` | 逐笔明细 | **做** | **不动**。明细就是明细,合并等于删信息 |

#### 6.1.4 · 复核纠正了什么

**清单从 22 个修正到 35 个块。五处修正:**

1. dashboard 漏了**账户多选筛选器**(不在目录里)→ **这是唯一被复核翻出来的真实缺口**,补 FR-486
2. dashboard 漏了**资产配置饼图**(藏在 `#dash-trend` 里,没独立 id)→ 结论仍是不动
3. reports 的 `#sec-structure` 我当成一个组件,**实际是 4 块** → 4 块结论都是不动
4. reports 的 `#sec-risk` 我当成一个,**实际是 2 张图** → 都不动
5. reports 的支出构成我只看了聚合部分,**下半部分还有逐笔明细行** → 明细类,不动

**但没有一条「不动」的判断被推翻。** 判据本身经受住了复核 ——
补进来的 12 个块,按「看 / 做 / 险」一分,归属都是清楚的、没有一个边界模糊。

#### 6.1.5 · 最终结论

**35 个块里 7 个受影响**,其中 2 个在本版早前已改(归因复盘 / 资产透视),
本轮改 5 个 —— 而 dashboard 分布图、dashboard 账户列表、reports 账户级收益
**共用同一份 `accountRows`**,所以真正的改动点只有 **3 处**:

| 改动点 | 覆盖组件 |
|---|---|
| `accountRows` 折叠(`AccountRowGrouper`) | dashboard #11 #12 · reports #16 |
| 封板贡献者折叠(`SealedPeriodService`) | reports #3 |
| 筛选器分组快选(纯前端) | dashboard #2 |

**盘查的价值不在于改得多,而在于知道哪些不能碰** —— 28 个块判为「不动」,
其中 3 个还得**主动在页面上说明为什么不动**(FR-485),否则用户会当成 bug。

### 6.2 · FR-482~486 · 这几个组件怎么改

#### FR-482 · 按账户分布图跟着分组走

横条图直接吃 `accountRows`,**改了数据源它自动就对了**,一行 JS 都不用动。
纯金额、无比率,**零口径风险**。TopN(Top10 + 「其他」)逻辑不变 ——
分组之后长尾变短,这个图本来就更好看了。

**真机验证**:建组前后**图上金额合计差 0**,「其他 8 个」变「其他 4 个」。

#### FR-483 · 账户列表按组折叠,但**比率类指标一律显示「—」**

这是这次唯一有**真实口径风险**的地方,单独说清楚。

账户列表有 11 个可选指标列。合并成组行时,它们**不是一类东西**:

| 能不能合 | 列 | 怎么算 |
|---|---|---|
| ✅ **可加** | 当前价值 · 累计损益 · 累计净投入 · 本期损益 · 本期Δ · 占比 | 直接相加 —— 都是同一本位币下的金额 |
| ✅ **可派生** | 本期Δ% | 合并后的 Δ金额 ÷ 合并后的上期余额 |
| ✅ **可重算** | 近况 sparkline | 成员的月末余额序列**按账期对齐相加**,再走同一个归一化函数 |
| ✅ **取最大** | 持有期数 | 组从最早那个成员开始算 |
| ❌ **不可加也不可平均** | **收益率(XIRR)· 本位币年化 · 最大回撤 · 预实** | **组行给 null,页面显示「—」** |

**为什么宁可给「—」也不给一个近似值:**

- **XIRR 是资金加权年化**。两个账户的 XIRR 不能相加,加权平均也不对 ——
  正确算法是把组内**所有现金流合并后重跑 XIRR**,而那需要 `AccountPeriodFact` 级的现金流,
  不在 `accountRows` 里。硬凑一个「按市值加权平均」会得到一个**看起来合理、实际没有数学意义**的数。
- **能不能用「累计损益 ÷ 累计净投入」顶替?** 不能。那是**累计收益率**,和 XIRR 的**年化资金加权**不是一个口径。
  把它填进「收益率」列 = **口径悄悄换了**,而页面上一个字都不说 ——
  这正是 memory `feedback_metric_refactor_baseline` 里那条教训的形状。
- **最大回撤更微妙**:在合并时序上重算,数学上做得到,但成员**开户/关户**会在合并曲线上造出一个
  跳变,回撤算法会把它当成波动 —— 得到一个**凭空出现的回撤**。不给,比给一个假的强。

**一个错的收益率比没有收益率危险得多:它看起来完全合理,没人会去质疑它。**

**代价说清楚**:建了组之后,组内账户各自的收益率在列表里看不到了。
所以 **FR-483b:组行必须能展开**,展开后是成员账户原样的行(所有指标齐全、账户名仍可点进详情)。
展开**不往返服务器** —— 成员行已经渲染在 DOM 里,只是 `display:none`。
这与归因区的「点开」(FR-476)是**同一个交互范式**,不另造一套。

#### FR-484 · 封板「最大变动账户」跟着分组走

「本期净资产变化的贡献者 Top N」。分组之后组内划转自动抵消 —— **这正是 issue #17 要的效果**。

两个必须做对的细节:

1. **本期与上期要用同一份分组关系折叠**。用各自期的关系,会让「改了分组」
   长得和「钱动了」**一模一样** —— 差额凭空冒出来,还说不清是哪来的。
2. 封板是**已关账期**,必须读 `period_account_group` 的**该期定格**关系(FR-468)。
   读当前成员关系的话,今天挪一个账户,历史封板页的贡献者列表会跟着变。

折叠放在**逐账户算完 delta 之前**:先把余额按标签汇总,再走原来那套 `isNew/isGone` 判定,
于是「组是不是本期新开的」自然等于「组里所有成员上期都没余额」—— 一行额外逻辑都不用写。

#### FR-485 · 「不跟组」的组件要**说明为什么**,而不是默默不跟

判定「不动」的 28 个块里,有三个用户**会觉得是 bug**:

| 组件 | 用户会怎么想 | 页面要说什么 |
|---|---|---|
| 集中度 HHI | 「我明明合并了,怎么还按账户算?」 | 「集中度按**真实账户**算 —— 分组是你的看法,不改变钱实际存在哪家机构」 |
| 完整性检查 | 「为什么这里还列着单个账户?」 | 「余额要**逐个账户**填 —— 组不是一个能记账的东西」 |
| 调仓计划下拉 | 「为什么选不到我的分组?」 | 「划转要落到**具体账户**上」 |

只在**该家建过组**时才显示这些说明 —— 没建组的用户不该看到一句多余的话(FR-466 零影响原则)。

#### FR-486 · 筛选器的「按分组快选」(复核补的)

**这个组件是复核时才发现漏了的** —— 它是个 `<details>` 表单、不在长文目录里,
所以第一遍按目录列清单时整个没看见。

建了「日常周转」之后,用户想「只看这一组」得**手动勾 5 个**,账户一多就会勾错或勾漏。

**刻意只做前端快选,不改筛选语义**:点一下把该组成员勾上,提交出去仍然是 `accounts=1,2,3…`。
理由是 —— 筛选器的语义是「**哪些账户**参与计算」,而组不是账户。
把组塞进这个语义里(比如加一个 `groups=` 参数)会让口径变糊,
而收益只有「少点几下」。**用一个纯前端的「帮你勾」拿到全部收益,零语义风险。**

行为上一个小决定:快选是**先全清再勾该组**,不是往已有选择上叠加 ——
「快选」读起来就是「就看这一组」。

### 顺带的待查项(不在本版 FR 内)

`pivot`(PivotEngine)与 `period_summary`(FactViewService)两条**代码上没有共用求和逻辑**的路径,
总资产会差 **0.01**,且**跟数据分布走**(历史上出现过 0.03 / 0.06)。不是任何一次改动引入的。

复现 issue #17 时它被再次触发,**刻意没有还原测试数据把它压绿** —— 那等于把真实的口径不一致藏起来。
分组 B 会大量动聚合路径,**是顺手收口的合适时机**,但先不进 FR,等 TDD 判影响面。

---

## 7 · 开关与默认值

**本版没有开关 —— 这是评审后的一处改进。**

| 能力 | 默认状态 | 与今天的差异 |
|---|---|---|
| 账户组 | **一个组都没有** | **逐字一致**(FR-466) |
| 分组 A 提示 | 常开 | 只增加解释,不改任何既有数字 |
| 分组 C 空态 | 常开 | 同上 |

原方案有一个「把活期账户合并统计」开关,评审时被否:**「有没有建组」本身就是开关**,
而且它比布尔开关表达力强得多(可以只合并 9 个中的 8 个)。

分组 A / C 不设开关 —— 给「说清楚」加一个能关掉的开关,没有意义。

---

## 8 · 隐私与数据红线

**不适用 · 本版不涉及 LLM 调用,也不新增任何对外暴露的接口。**

一处需要留意:分组 A 的提示文案里会出现**账户名与金额**。
它只在**已登录的填报页**渲染,与该页既有内容同级 —— 不进日志、不进审计表、不进任何截图产物。
截图规范照旧(隐私模式默认开)。

---

## 9 · 失败模式(pre-mortem)

**① 折叠漏掉某条聚合路径 —— 而且不报错。**
四条路径(归因 / 收益 / 透视 / 分布)只改了三条,第四条仍按账户展开。
表现是「同一屏上两块数对不上」,或者更糟:**看起来完全正常,只是某一块没折叠**。
**怎么发现**:grep 护栏扫「所有按账户聚合的入口是否都过了同一个折叠层」;
零组态与 tag `v1.19.16` 的逐字比对。

**② 单属被破坏,恒等式静默失衡。**
只要有一条写入路径漏了唯一性校验(批量导入、数据修复、并发建组),同一账户进两个组,
合计就会大于总额 —— 而归因会把差额**吸进「未归因」**,数字看着平了,错误藏起来了。
**怎么发现**:DB 唯一约束(不能只靠应用层校验)+ 恒等式断言进 e2e。

**③ 组内流转的判定用错了「当时」还是「现在」。**
一笔 2026-03 的划转,当时两端不同组、今天同组了 —— 按哪个算?
FR-468 说按**该期定格的成员关系**算。写成「按当前成员关系」的话,**历史收益率会随建组而变**,
用户会当成算错了。
**怎么发现**:e2e —— 建组后已关账月份的历史数字必须一字不变。

**④ 护栏又绑在偶然事实上。**
本项目已经犯过四次(`v08-NAV-1` 绑单个路径 · `v181-FLOAT-DOCK` / `v1617-FIVE` 绑三按钮数组 ·
`v11911` 数「出现两次」把收口误判成退化)。分组 B 最容易犯 —— 比如写死「折叠后应该是 4 行」。
**怎么发现**:判据守**设计意图**(所有聚合入口共用同一个折叠层),不守当下的数量或写法。

**⑤ 分组 A 的提示变成噪声。**
每改一笔收入都弹一次,用户三天后开始无视它 —— 那这一版就白做了。
**怎么发现**:触发条件必须**窄到可枚举**(该账户本期已有校准记录 **且** 本次改动会改变余额),
并把正反两面都写成 e2e 断言(该出现的出现、不该出现的不出现)。

---

## 10 · 验收 → 护栏映射

| # | 验收标准 | 机器可校验? | 落在哪 |
|---|---|---|---|
| 1 | 在当月已校准余额的账户上删一笔收入,**当场**看到提示,且含校准时间与前后余额 | 是 | `e2e` 新增主线 |
| 2 | 提示**不阻断**:点「知道了」之后改动照常生效 | 是 | `e2e` 同主线 |
| 3 | 在当月**未填余额**的账户上改收支,页面明确说明这笔改动不进本期净资产变化 | 是 | `e2e` 同主线 |
| 4 | 提示**不该出现时不出现**(该账户本期从未校准 / 本次改动不改变余额) | 是 | `e2e` 反面断言 |
| 5 | **零组态**下,所有页面与 tag `v1.19.16` **逐字一致** | 是 | `qa-run` 新护栏 · 零组态快照比对 |
| 6 | 四条聚合路径**全部**过同一个折叠层(不许有旁路) | 是 | `qa-run` grep 护栏 · **守意图不守数量** |
| 7 | 一个账户**不可能**同时属于两个组 | 是 | **DB 唯一约束** + 单测 |
| 8 | 组内互转一笔钱,该组的收益率与现金流**不受影响**;转给组外则受影响 | 是 | `e2e` |
| 9 | 建组 / 改组成员后,**已关账月份的历史数字一字不变** | 是 | `e2e` · 建组前后快照比对 |
| 10 | 删组之后,底层数据**零变化**,页面回到未分组的样子 | 是 | `e2e` · DB 校验和比对 |
| 11 | 组默认**折叠**呈现,且能下钻到成员账户 | 是 | `e2e` + 截图 |
| 12 | 归因空态下,没读过文档的人能说出「它在回答什么」「下一步做什么」 | **否** | 双端截图 + UED 视角人工核对 · 理由:可理解性无法自动判定 |
| 13 | PC + 移动双端排版 | **否** | 双端截图人工核对 · 理由:排版类 |

---

## 11 · 回滚与关闭

- **关开关够不够**:本版**没有开关**。等价的退路是**删掉所有组** —— FR-463 保证删组后数据一行不动、
  页面回到未分组的样子。这条比开关强:它是**用户自己就能做**的退路。
- **DB 迁移**:**有** —— 账户组 + 成员关系 + 成员关系的账期定格,**只加表不改既有表**。
  回滚 = 删表,既有数据零影响。**这个迁移必须可回滚**,TDD 阶段给出回滚脚本。
- **不可逆动作**:**无**。不发送任何对外请求、不在第三方侧创建或删除任何东西、不重写历史数据。
- **prod 回滚路径**:`deploy/rollback.sh` + `app.jar.prev`(24 小时保留窗口)。
  注意:jar 回滚后新表仍在但没人读,**不影响旧代码运行** —— 这正是「只加表不改表」的好处。

---

## 13 · 验收反馈修正(2026-09-08 · 维护者验收 beta 后)

维护者在 beta 上走了一遍,提了四条。**其中两条是我实现里的真缺陷**,另外两条是信息架构没想清楚。

| # | 反馈原话 | 判定 |
|---|---|---|
| 1 | 「账户分组为什么还需要独立的管理页面,是否应该和账户管理想办法融合」 | **信息架构错了** |
| 2 | 「在账户列表详情页,应该增加去『管理账户分组』的入口」 | **可发现性为零** |
| 3 | 「分组管理页的交互怎么这么奇怪」 | **界面没法用** |
| 4 | 「如果同一个账户同时属于两个分组,直接报错 sql 错误」 | **两个缺陷被混成一条** |

### FR-471 · 分组归到「账户」域下,不再是一个孤立的管理页

第一版把分组做成了 `/admin/account-groups`。这是错的:
**分组回答的是「这些账户合起来看」,和账户列表是同一件事的两个方向** ——
「从账户看它属于哪个组」和「从组看它含哪些账户」。
放在管理页,等于让用户先想到「这是一项配置」才找得到它,
而实际上他是**在看账户的时候**想起来的。

搬到 `/accounts/groups`,入口三处:账户列表页顶部按钮、账户详情页的分组 pill、管理页保留一张卡片。

### FR-472 · 账户列表与详情页要显示「所属分组」

列表加一列(未分组显示「未分组」,同样可点);详情页与「类目」同级加一个分组 pill。
**没有这个,用户只有恰好翻到管理页才会知道有这个能力。**

### FR-473 · 分组页:折叠列表 + 搜索,而不是把全部账户平铺 N 遍

第一版每个分组都渲染**全部 19 个账户**的 checkbox,N 个组就是 N 份 —— 维护者原话「怎么这么奇怪」。
改成:分组卡片**默认折叠**(组名 + 成员数 + 成员名摘要),点开才展开编辑;
顶部可按**分组名或成员账户名**搜索(命中即自动展开);另有全部展开/收起。

### FR-474 · 冲突要给人话,而不是让 DB 约束冒成 500

复现下来,维护者说的「sql 错误」其实是**两件事**:

| 现象 | 真相 |
|---|---|
| **500 白页** | 根因是**组名重复**(`Duplicate entry '1-组A' for key 'uk_group_family_name'`),不是账户冲突 |
| 账户被两个组选中 | **不报错** —— 而是**悄悄把账户从原组搬走**(第一版 `ON DUPLICATE KEY UPDATE` 是搬家语义) |

**第二件比第一件更糟。** 原组的成员少了一个、它的收益口径当场就变了,而页面一个字都不说。
**静默地改掉用户没打算改的东西,比报错更糟。**

| # | 要求 |
|---|---|
| FR-474a | 组名重复 → 业务异常「已经有一个叫『X』的分组了,换个名字」,不是 500 |
| FR-474b | 账户已属别组 → **拒绝**,并说清被谁占了、怎么挪过来。**绝不静默搬走** |
| FR-474c | 界面上这类账户**置灰 + 禁选 + 标出「已在『X』」**,别让用户走到提交才被拒 |
| FR-474d | 绕过界面直接调接口,同样得到人话而不是约束名 |

### FR-475 · 分组页:主页面只有列表,新建走弹窗(二次验收)

维护者第二轮原话:

> 「进入分组管理,就是一个分组的列表,**没有推荐**;分组列表页**右上角有新建**,
>  新建是**弹窗**,才有现在的『推荐』里面的内容;同时**分组名也要允许用户自己维护**。」

第一版把推荐做成了列表页上的常驻卡片墙。错在**把「建的时候的快捷方式」当成了常驻功能** ——
用户进这一页十次有九次是来**看**已有分组的,推荐只在第十次有用,却占着最好的位置。

| # | 要求 |
|---|---|
| FR-475a | 主页面 = **分组列表 + 搜索**,没有别的 |
| FR-475b | 右上角「+ 新建分组」→ **弹窗**(`<dialog>`),推荐收在弹窗里 |
| FR-475c | 推荐只**预填**名字与成员,**两者都还能改** —— 分组名由用户自己维护,不是推荐写死 |
| FR-475d | 空态例外:一个组都没有时,列表位置放简短说明 + 「新建第一个分组」 |

### FR-476 · 归因区要有「点开」

第一版的下钻只有 `?expand=组ID` 这个 query,**页面上没有任何入口** ——
等于「做了但没人找得到」,和把分组藏进管理页是同一个毛病。

| # | 要求 |
|---|---|
| FR-476a | 「赚得最多 / 亏得最多」里,维值是**分组**时给一个「点开」;是账户时不给(它已经是最细的) |
| FR-476b | 展开态顶部给横幅说清「已展开分组」+ 「收起」 |
| FR-476c | 走 HTMX 换片段,与维度切换同一条路;事件用**委托**绑(这两块是 innerHTML 重绘的,直接绑会在下次重绘时丢) |

### FR-477 · 勾选账户时,光有名字不够辨别

维护者:「新建分组的弹框,账户的信息太少了,把账户的**类型**和 **owner** 都要展示出来。」

家里两张卡都叫「招行」是常事,而「哪张是我的、哪张是老婆的」**恰恰是决定要不要放进同一个组的依据**。
每行改成「类型 pill · 账户名 · 主理人」,新建弹窗与编辑现有分组两处都给。

主理人取**含已归档成员**的目录 —— 账户不会因为主理人被归档就换主人,
用仅活跃列表会让名字变成「成员#7」(v1.15 FR-382 踩过同一个坑)。

### FR-478 · 推荐里少了 issue #17 提出者真正要的那一组

原来只推「随时可取」和「负债」。但提出者的原话是:

> 「我实际有非常多个活期账户,有不同**银行卡**的、有**微信**的、有**支付宝**的,
>  经常会需要在这些不同的活期账户中转账。」

「随时可取」是**流动性分层**(LIQUID),把货币基金类理财也算进来了 —— **不是一回事**。
他说的「活期账户」指的是**互相转账的那些钱包**。货基和它们之间很少来回划转,并进来只会让组的收益口径变糊。

新增推荐 **「日常周转」= 所有 CASH 类型账户**,放在第一位;「随时可取」保留,判据写在卡片上让用户自己挑。

### FR-479 · 「建这个分组」根本没渲染成按钮

维护者:「『建这个分组』太不像一个按钮了。」

**根因不是样式没调好,是这个 CSS 类不存在。** 我写的 `btn-primary` 全项目从未定义过
(实际有的是 `btn-ink` / `btn-paper` / `btn-ghost`),浏览器对不存在的 class **完全不吭声**,
于是它静静渲染成一行纯文字 —— 编译、单测、qa-run **全绿**,只有人眼能发现。

同一个错还在 v1.19 的 `admin/ai-access.html` 里躺着(也是我写的),一并修掉。
落护栏 `v1200-NO-PHANTOM-BTN-CLASS`:扫模板里所有 `btn-*`,逐个回查 `style.css` 有没有定义 ——
**把人眼那一步机械化**。

---

## 12 · 来源与参考

- **原始需求**:[issue #17](https://github.com/LuoDi-Nate/financial-management/issues/17) · 提交者 @brdol · 2026-09-05
- **已剥离**:[issue #18](https://github.com/LuoDi-Nate/financial-management/issues/18) · 自定义支出分类
- **已发布的相关补丁**:v1.19.16(AI 复盘缓存失效)—— 见 `tech-design/v1.19.md` 附录十三、
  `docs/qa-cases.md` 的 `v11916-*` 与 `KNOWN-PIVOT-VS-KPI-ROUNDING`
- **复现记录**:beta 真实浏览器操作 + DB 前后比对(截图未入库 —— 含 beta 测试数据金额)
- **评审记录(2026-09-07)**:维护者把「活期合并」抽象为「账户组」;
  否掉「真合并成可记账账户」与「按比例分摊」两条路;确认单属、组非账户属性、
  账户维度按组折叠渲染(默认折叠 + 可下钻)、跨类型不拦但提示
- **相关既有设计**:`AGENTS.md` 领域概念节 · `prd/v1.10.md`(报表封板与仪表盘实时的分工)·
  `prd/v1.12.md`(重开后再关账 = 重新定格 —— FR-468 复用这条规则)
